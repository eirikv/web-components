{
  "name": "web-components",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node esbuild.config.mjs",
    "start": "node esbuild.config.mjs --server",
    "prettier": "prettier -w src"
  },
  "dependencies": {
    "lit": "^2.4.0"
  },
  "devDependencies": {
    "esbuild": "^0.15.12",
    "prettier": "^2.8.0",
    "typescript": "^4.8.4"
  }
}
